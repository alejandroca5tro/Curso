// Ejemplo del controller utilizando inyección del $scope
// como era más habitual en los inicios de AngularJS

angular.module("miApp", []) //los corchetes vacíos son el inyector de dependencias. Esto instancia el módulo.

//sin el inyector de dependencias, lo que se haría sería buscar al módulo.